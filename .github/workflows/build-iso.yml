name: Build ISO
on:
  workflow_run:
    workflows: [bluebuild]
    types: [completed]
    branches: [main]
  workflow_dispatch:
jobs:
  build-iso:
    name: Build Anaconda ISO (${{ matrix.image_name }})
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        image_name:
          - mayfield-os
          - mayfield-os-nvidia
    steps:
      - name: Build ISO
        uses: jasonn3/build-container-installer@9c889bd0cf7c8fabf375a24e8d113a1212a32413
        with:
          image_repo: ghcr.io/${{ github.repository_owner }}
          image_name: ${{ matrix.image_name }}
          image_tag: latest
          variant: Kinoite
          version: "44"

      - name: Upload ISO as artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ${{ matrix.image_name }}-iso
          path: build/deploy.iso
          if-no-files-found: error
          retention-days: 7
