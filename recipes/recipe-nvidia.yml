# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

name: mayfiled-os-nvidia
description: Mayfiled OS (NVIDIA) - A custom Aurora-based desktop image with NVIDIA drivers.

base-image: ghcr.io/ublue-os/aurora-nvidia
image-version: stable

modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: dnf
    install:
      packages:
        - distrobox
        - htop
        - kate
    remove:
      packages:
        - firefox
        - firefox-langpacks

  - type: default-flatpaks
    configurations:
      - notify: true
        scope: system
        install:
          - org.mozilla.firefox
      - scope: user
        install:
          - us.zoom.Zoom
          - com.spotify.Client
          - org.kde.kpat

  - type: signing
